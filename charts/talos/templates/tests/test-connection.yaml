apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "talos.fullname" . }}-test-connection"
  labels:
    {{- include "talos.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "talos.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
